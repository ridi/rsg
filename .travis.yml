language: node_js
node_js: '8'

stages:
  - name: build
  - name: deploy

jobs:
  include:

    - stage: build
      script: npm build

    - stage: deploy
      before_script: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
      script: npm publish
