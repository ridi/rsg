language: node_js
node_js: '8'
script: npm build
before_deploy:
  - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
deploy:
  provider: npm
  email: performance.team@ridi.com
  on:
    repo: ridi/rsg
    branch: setup-ci
  skip_cleanup: true
  script: npm publish
